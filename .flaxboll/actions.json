{"1764937807901":{"actionType":"saveAll","createdAt":"1764937807901","actionID":"1764937807901","userID":"#TEMP","path":"system.capsules.bf78c698-3fce-43e7-aebb-dc4139a29a4b.capsUseCodeInfo.capsUseInputs.sss_custom_caps-0.capsIptTxtValue","oldValue":"(txt) => {\n  try {\n    if (typeof txt !== \"string\") txt = String(txt ?? \"\");\n\n    // Mantém apenas números (sem regex)\n    let clean = \"\";\n    for (let i = 0; i < txt.length; i++) {\n      const ch = txt[i];\n      if (ch >= \"0\" && ch <= \"9\") {\n        clean += ch;\n      }\n    }\n\n    // Limita a 8 dígitos (ddmmyyyy)\n    if (clean.length > 8) clean = clean.slice(0, 8);\n\n    console.log({ clean });\n\n    // Monta máscara dd/mm/aaaa\n    let masked = \"\";\n    if (clean.length > 0) masked = clean.slice(0, 2);         // dd\n    if (clean.length >= 3) masked += \"/\" + clean.slice(2, 4); // dd/mm\n    if (clean.length >= 5) masked += \"/\" + clean.slice(4, 8); // dd/mm/aaaa\n\n    console.log({ masked });\n\n    tools.functions.setVar({\n      args: \"\",\n      pass: {\n        keyPath: [\"sc.a1.iptChanges.startDate\"],\n        value: [String(masked)],\n      },\n    });\n  } catch (e) {\n    console.error(\"Erro na máscara de data:\", e);\n    return txt;\n  }\n}","newValue":"(txt) => {\n  try {\n    if (typeof txt !== \"string\") txt = String(txt ?? \"\");\n\n    // Mantém apenas números (sem regex)\n    let clean = \"\";\n    for (let i = 0; i < txt.length; i++) {\n      const ch = txt[i];\n      if (ch >= \"0\" && ch <= \"9\") {\n        clean += ch;\n      }\n    }\n\n    // Limita a 8 dígitos (ddmmyyyy)\n    if (clean.length > 8) clean = clean.slice(0, 8);\n\n    console.log({ clean });\n\n    // Monta máscara dd/mm/aaaa\n    let masked = \"\";\n    if (clean.length > 0) masked = clean.slice(0, 2);         // dd\n    if (clean.length >= 3) masked += \"/\" + clean.slice(2, 4); // dd/mm\n    if (clean.length >= 5) masked += \"/\" + clean.slice(4, 8); // dd/mm/aaaa\n\n    console.log({ masked });\n\n    tools.functions.setVar({\n      args: \"\",\n      pass: {\n        keyPath: [\"sc.a5a.iptsChanges.date\"],\n        value: [String(masked)],\n      },\n    });\n  } catch (e) {\n    console.error(\"Erro na máscara de data:\", e);\n    return txt;\n  }\n}"}}